{
  "name": "hue-control-integration",
  "version": "1.0.0",
  "description": "Integration API towards hue bridge",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/esbuild/bin/esbuild src/server.js --platform=node --bundle --outfile=dist/app.cjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "author": "Jesper Thörnberg",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.20.0",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "husky": "^7.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.14.28"
  }
}
